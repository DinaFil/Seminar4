void FillArray(int[] collection)

{
int length = collection.Length;
int index = 0;
while (index < length)
{

index++;
}

}

int array [] = new int [10]; //создать новый массив, имеющий 10 элементов, по умолчанию, наполненный нулями.
